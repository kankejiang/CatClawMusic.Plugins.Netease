- 20:48-20:55 修复点击登录闪退（NRE @ NeteaseOnlineMusicPage.cs:403 OnAccountTapped）：根因=宿主桌面端无 Shell（窗口直连不走 Shell 导航栈）→ `Shell.Current.GoToAsync("webviewlogin?platform=netease")` NRE（其他 Shell.Current 调用都有 try/catch 兜底，唯独这行裸用）。修复（两侧）：
  ① 插件 NeteaseOnlineMusicPage.cs：Shell.Current 判空 → 回退 `_services.GetService<INavigationService>().NavigateToAsync(...)` → 再兜底 ShowTip（using 补 CatClawMusic.Core.Interfaces）。
  ② 宿主 NavigationService.cs：桌面无 Shell 时 `OpenEmbeddedByRoute`——route 拆查询参数（webviewlogin?platform=netease → platform=netease）+ 显式 `_embeddedRoutes` 字典（webviewlogin→WebViewLoginPage，MAUI 无按 route 取 type 的公开 API，Routing.GetRoute(BindableObject) 签名不符编译报错）→ DI 取页 → ApplyQueryAttributes 注入 IQueryAttributable → DesktopNavigation.OpenEmbedded 嵌入主区域。
   提交：插件 79dbb06（main，已 push）；宿主 3ceb4f6（master，已 push）。插件 .ccp 验证通过（OnAccountTapped #Strings UTF-8 / 路由串 #US UTF-16LE / INavigationService 引用存在）→ 覆盖上传 v0.2.0（补丁级不新建 Release，Version 不变，172032 字节）。
- 21:30-21:40 修复桌面端点击登录进入空白页：根因=宿主 `DesktopBlankPage.OpenEmbeddedPage` 嵌入模式只反射调 OnAppearing、从不调 OnNavigatedTo，而 `WebViewLoginPage` 只在 OnNavigatedTo 里给 WebView 设 Source → 嵌入时 WebView 永远空白。修复（宿主 WebViewLoginPage.xaml.cs，未提交）：抽 `EnsureLoginLoaded()`（`_loginLoadStarted` 幂等）+ OnAppearing 兜底调用（Shell/Android 下 OnNavigatedTo 已加载则跳过）；OnBackTapped 补 StopCookiePolling（桌面嵌入不触发 OnNavigatedFrom/OnDisappearing，定时器会泄漏）。宿主 windows TFM 编译通过（0 错误）。教训：宿主所有桌面嵌入页的生命周期以 OnAppearing 为准，页面初始化逻辑必须放 OnAppearing 或双入口幂等。
- 21:45-21:47 修复桌面端返回按钮首轮 NRE（`Shell.Current.Navigation.PopAsync()` @ 插件三个页面的返回按钮）：桌面无 Shell 时 Shell.Current 为 null → NRE（被空 catch 吞掉，VS 显示首轮异常，按钮实际失效）。修复：三处（NeteaseOnlineMusicPage/CreateBackButton、NeteaseAlbumPage、NeteaseArtistPage）改为 Shell.Current 判空 → 回退 `_services.GetService<INavigationService>().GoBackAsync()`（Album/Artist 补 using CatClawMusic.Core.Interfaces）。提交 989a141（main，已 push）→ .ccp 覆盖上传 v0.2.0（172544 字节，digest 2b5524a2）。
- 22:00-22:15 修复"登录后右上角不显示用户名/不弹退出确认"：实测 %LOCALAPPDATA%\CatClawMusic.Maui\netease_cookie.txt 被匿名 Cookie（NMTID/HMACCOUNT，无 MUSIC_U）覆盖（21:43:50）→ 登录状态丢失。根因=宿主 WebViewLoginPage"完成"按钮 `TryExtractCookieAndReturnAsync` 只判 cookie 非空就回传，未登录时的匿名 Cookie 也能通过 → 覆盖插件已登录 Cookie 文件。修复（两侧）：① 宿主 WebViewLoginPage.xaml.cs：回传前校验 `SuccessCookieNames`（MUSIC_U）全存在，否则仅提示不回传（未提交）；② 插件 NetEaseOpenApiClient.cs：`HasCookie` 改为必须含 "MUSIC_U="；`ApplyLoginCookieAsync` 无 MUSIC_U 直接忽略；`GetAccountNameAsync` 用 HasCookie 判断。提交 b4578e6（main，已 push）→ .ccp 覆盖上传 v0.2.0（172544 字节，digest e97580ab）。本机已删除被污染的 cookie 文件，用户需重新登录。
- 22:20-22:30 修复"点用户名不弹退出确认"：桌面嵌入模式下插件页无 Window，`Page.DisplayAlert` 在 Windows 上不弹窗（MAUI 10 还标记废弃）。修复：`OnAccountTapped` 退出确认改用 `_services.GetService<IDialogService>().ShowConfirmAsync`（宿主 DialogService 走 `Application.Current.MainPage.DisplayAlert`，MainPage=DesktopBlankPage 有 Window）；Artist/Album 页的提示框同样改用 IDialogService（fallback DisplayAlertAsync）。提交 62eaf16（main，已 push）→ .ccp 覆盖上传 v0.2.0（173056 字节，digest c3a3fca2）。教训：插件页在桌面嵌入模式下不要用 Page 自身的弹窗 API，统一走宿主 IDialogService。
- 22:40-22:55 桌面宽屏响应式布局（提交 3681256，main，已 push；.ccp 覆盖上传 v0.2.0，175616 字节，digest ab1151cd）：修复歌单网格只占左半屏（根因=`SizeChanged` 挂在页面级，桌面嵌入模式下页面脱离视觉树永不触发，span 停在初值）→ 改挂 `contentGrid.SizeChanged`。新增 `ApplyResponsiveLayout(w)`（阈值 <600→2 … ≥1500→6）+ 宽屏（≥900）`ApplyWideLayout`：搜索框与 chips 同行（Star|Auto）、入口卡片一行五列（entryContainer 从 VerticalStackLayout 改 Grid，只重设行列归属不重建控件）、歌曲/歌手列表切 GridItemsLayout(2) 双列通栏；`ApplyNarrowLayout` 恢复原状，Android 窄屏布局不变。注意：响应式引用的控件（searchRowGrid/searchBorder/searchModesScroll/searchModesLayout/entryContainer/5 张入口卡）已从构造函数局部变量提升为 readonly 字段。
- 23:16 发布 v0.3.0（minor 跳变 → 新建 Release）：Version 0.2.0→0.3.0（NetEaseMusicPlugin.cs:31），提交 b8c7cf2（main，已 push），gh release create v0.3.0（.ccp 175616 字节，digest 08849c75）；删除旧 v0.2.0 Release（连 tag），只留最新。Release 说明覆盖：宽屏响应式布局、登录闪退、登录页空白、匿名 Cookie 覆盖、返回按钮、退出弹窗。
- 次日 21:51 拉取 c4fe759（feat: 封面改内存流下载 OnlineUrlToStreamImageConverter，不落盘缓存）后重新构建并 --clobber 覆盖上传 v0.3.0（.ccp 178176 字节，digest 353b016f）。
- 08-15 14:00-14:40 排查 Android 模拟器 JNI 崩溃（"Could not register native methods for class 'crc64.../ShellItemRenderer'（或 'net/dot/jni/ManagedPeer'）；JNIEnv::RegisterNatives() returned -1"）：**根因=这两台 Houdini ARM 转译的 x86_64 Android 12 模拟器（10.0.0.12/.13）跑不了 .NET Android Debug 运行时（libmono-android.debug.so）**，Debug 无论 x64 原生还是 arm64 转译、无论 AndroidEnableMarshalMethods/AndroidIncludeDebugSymbols 怎么调都崩；**Release（libmono-android.release.so）正常**。宿主 75c213e 的 keystore 修复是红鲱鱼（保留无害）。排查要点：APK 缺 libmonodroid.so 是首次 CLI 构建 XABAA7000 失败留下的脏 obj 产物，clean 后重建即恢复；Release 1.8.1 双 ABI 包实测加载 lib/arm64（Houdini 转译）运行正常；`dotnet build -r android-x64 -c Release` 产物（89MB）装两台模拟器均正常。结论：**模拟器测试用 Release 构建（VS F5 Debug 部署到真机）**；发布走 build-release.ps1 或 `dotnet build -c Release`。CLI 构建 Android 注意：单 RID 产物在 bin\...\android-x64\ 子目录；Debug 下 csproj 的 AutoAndroidRid 目标会把 RuntimeIdentifiers 改 x64-only，多 RID 与单 RID 切换易出脏产物。
- 08-15 15:10-15:40 通用插件快捷入口机制（IQuickEntryPlugin，宿主 a410373 + 插件 3605762）：宿主发现页 HeroTrack 布局 = AI 卡(0) + 插件快捷入口卡(1..n) + 英雄卡(n+1..)。Core 新增 Interfaces/IQuickEntryPlugin.cs（QuickEntryInfo{Id,Title,Icon,Subtitle,Color1,Color2} + 接口），PluginAdapters.cs 加 QuickEntryAdapter（按属性名反射读取，跨版本安全，未实现方法静默降级），PluginManager 全限定名匹配注册。DesktopDiscoverPage：RebuildHeroTrack 在 AI 卡后遍历 GetEnabledPlugins<IQuickEntryPlugin>() 渲染入口卡，点击 → ExecuteQuickEntry(entryId) → 按 PluginId 匹配 IViewContributorPlugin → OpenPluginEntryAsync 打开插件页；英雄卡起始下标改 `_heroStartIndex`（1+快捷卡数），ScrollHeroTo/OnHeroScrolled 用它做偏移。插件侧：NetEaseMusicPlugin 实现 IQuickEntryPlugin 注册"私人漫游"（Id=fm），ExecuteQuickEntry 置 `_pendingFm`；VM.OnAppearingAsync 末尾 `ConsumePendingFm()` 为真则 LoadPrivateFmAsync 自动开播。DiscoverPageBase.OpenPluginEntryAsync 改 protected。**设计原则（用户要求）：快捷入口必须是通用口子，做新插件不能改宿主，老插件未实现该接口自动不显示**。另修复 SearchPage.xaml.cs 手势代码缺 #if ANDROID 导致 Windows 全量编译失败（c568f1a 引入）。插件 .ccp 覆盖上传 v0.3.0（182272 字节，digest 473c126b）。
- 08-15 15:50 快捷入口排序规则（宿主 a2f9308 + 插件 21fe99b）：QuickEntryInfo 增加 `SortOrder`（int，升序越小越前，默认 0）；宿主按 (SortOrder, 插件注册顺序) 稳定排序——**并列权重按注册顺序先注册在前，确定性不冲突**；QuickEntryAdapter 反射读取 SortOrder（旧版本无该属性默认 0）。网易云"私人漫游"显式 SortOrder=0。.ccp 覆盖上传 v0.3.1（182272 字节，digest 543bed79）。
- 08-15 16:20 快捷入口直接执行（宿主 f309a9c + 插件 e44f81e）：`ExecuteQuickEntry(entryId, IServiceProvider services)`——宿主把服务传给插件、**不再自动打开插件页**，执行权完全交给插件（是否开页由插件决定）。网易云"fm"入口点击 → `GetSharedVm(services)`（从 CreateEntryPage 抽出单例创建辅助方法）→ `vm.LoadPrivateFmAsync()` 直接开播，不进页面；移除 `_pendingFm`/`ConsumePendingFm` 待启动标记机制及 VM.OnAppearingAsync 消费逻辑。.ccp 覆盖上传 v0.3.1（182272 字节，digest aad67432）。
- 08-15 16:50 手机端（安卓竖屏）快捷入口（宿主 134bf54）：SearchPage.RebuildHeroDisplay 在 AI 卡后按 (SortOrder, 插件注册序) 插入快捷入口卡（HeroCardItem，Tag 徽章=entry.Icon，渐变用 Color1/Color2）；**路由用对象引用作 key**（`_quickEntryMap: Dictionary<HeroCardItem, (plugin, entry)>`，Tag 只作显示文本避免跨插件徽章撞 key）；OnHeroCardTapped/OnHeroPlayTapped 在 AI 分支后查 map → ExecuteQuickEntry(entry.Id, Services)。Release 装 10.0.0.13 模拟器验证运行正常。
